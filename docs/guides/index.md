# Guides

Follow these guides to configure your compute backend ({doc}`Docker </guides/docker>` or {doc}`Modal </guides/modal>`) and then {doc}`run </guides/run_provers>` and {doc}`benchmark </guides/benchmark>` provers. Each guide assumes you have already installed `open-atp` (see {doc}`../installation`).

For development guides, see {doc}`/development/index`.

```{toctree}
:caption: Guides
:maxdepth: 1

docker
modal
run_provers
benchmark
```
